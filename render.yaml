services:
  - type: web
    name: us-iran-war-dashboard-financial
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HOST
        value: 0.0.0.0
